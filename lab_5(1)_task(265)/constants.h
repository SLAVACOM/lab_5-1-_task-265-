#pragma once
#include <string>

using namespace std;

namespace constants {
    const string input { "rez/in.txt" };
    const string output { "rez/out.txt" };

    const char START_SYMBOL = '*';
    const char REPLACE_SYMBOL = '-';
}